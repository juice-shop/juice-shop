name: Compress images
on:
  pull_request:
    paths:
      - '**.avif'
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'
  push:
    branches:
      - master
      - develop
    paths:
      - '**.avif'
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'
jobs:
  build:
    name: calibreapp/image-actions
    runs-on: ubuntu-latest
    if: |
      github.repository == 'juice-shop/juice-shop' &&
      (github.event_name != 'pull_request' ||
       github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout Branch
         uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608
      - name: Compress Images
        id: calibre
         uses: calibreapp/image-actions@8ade135a41bc03ea155e62e844d188df1ea18608
        with:
          githubToken: ${{ secrets.GITHUB_TOKEN }}
          ignorePaths: '**/3d_keychain.jpg,**/favorite-hiking-place.png,**/5.png'
          compressOnly: ${{ github.event_name != 'pull_request' }}
      - name: Create Pull Request
        if: |
          github.event_name != 'pull_request' &&
          steps.calibre.outputs.markdown != ''
         uses: peter-evans/create-pull-request@8ade135a41bc03ea155e62e844d188df1ea18608
        with:
          title: Auto Compress Images
          delete-branch: true
          commit-message: Compress Images
          body: ${{ steps.calibre.outputs.markdown }}
